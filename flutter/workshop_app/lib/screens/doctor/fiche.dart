import 'package:flutter/material.dart';
class Fiche extends StatelessWidget {
  const Fiche({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('thats me'),
    );
  }
}

